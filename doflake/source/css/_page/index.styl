.body-container {
    background-color: rgba(105, 112, 111, 0.1);
}

.body-main {
    width: 90%;
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: stretch;
}